# Local development / training environment.
# The ClearML Agent recreates this list automatically from the captured task,
# so these versions are what your experiments will run with remotely too.
clearml>=2.1
clearml-agent>=2.0
scikit-learn>=1.5
pandas>=2.0
matplotlib>=3.7
joblib>=1.3
# UI
streamlit>=1.30
requests>=2.31
